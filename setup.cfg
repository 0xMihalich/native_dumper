[metadata]
name = native-dumper
version = 0.3.2.3
description = Library for read and write Native format between Clickhouse and file.

[options]
packages = find:
package_dir =
    = src
install_requires =
    light-compressor==0.0.1.9
    nativelib==0.2.2.1
    sqlparse>=0.5.3

[options.packages.find]
where = src
